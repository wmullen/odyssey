{{! Sheet Header }}
<header class='sheet-header'>
  <img
    class='profile-img'
    src='{{actor.img}}'
    data-edit='img'
    data-action='onEditImage'
    title='{{actor.name}}'
    height='100'
    width='100'
  />
  <div class='header-fields'>
    <div class='document-name'>
      <input
        name='name'
        type='text'
        value='{{actor.name}}'
        placeholder='Name'
      />
    </div>
    {{! The grid classes are defined in scss/global/_grid.scss. To use,
      use both the "grid" and "grid-Ncol" class where "N" can be any number
      from 1 to 12 and will create that number of columns.  }}
    <div class='resources grid grid-3col'>

      {{! "flex-group-center" is also defined in the _grid.scss file
        and it will add a small amount of padding, a border, and will
        center all of its child elements content and text. }}
      <div class='resource flex-group-center'>
        <label for='system.harm.value' class='resource-label'>
          Harm
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            name='system.harm.value'
            value='{{system.harm.value}}'
            data-dtype='Number'
          />
          <span> / </span>
          <input
            type='text'
            name='system.harm.max'
            value='{{system.harm.max}}'
            data-dtype='Number'
          />
        </div>
      </div>

      <div class='resource flex-group-center'>
        {{#each system.stats as |ability key|}}
          <div class="ability flexrow flex-group-center">
            <label for="system.stats.{{key}}.value" class="resource-label">{{key}}</label>
            <input type="text" name="system.stats.{{key}}.value" value="{{ability.value}}" data-dtype="Number" placeholder="{{ability.value}}"/>
          </div>
        {{/each}}
      </div>
    </div>
  </div>
</header>